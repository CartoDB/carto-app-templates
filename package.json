{
  "name": "carto-app-templates",
  "private": true,
  "packageManager": "yarn@4.2.2",
  "workspaces": [
    "templates/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "preview": "lerna run preview"
  },
  "devDependencies": {
    "lerna": "^8.1.6",
    "typescript": "^5.5.3"
  }
}
